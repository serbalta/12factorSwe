spring:
  boot:
    admin:
      client:
          url: http://localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: "*"



spcloud:
  cassandra:
    contact:
      point: localhost
    port: 9042
    keyspace:
      name: springcloud

    username: cassandra
    password: cassandra

